server:
  port: 8080
spring:
  application:
    name: trucks
  mongodb:
    uri: ${MONGO_DB_URL}
    database: trucks
management:
  server:
    port: 9090
  endpoints:
    web:
      exposure:
        include: health, info, prometheus