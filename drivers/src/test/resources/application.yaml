spring:
  application:
    name: drivers
  security:
    oauth2:
      resourceserver:
        jwt:
          # Atrapa - testy podstawiaja gotowe tokeny (SecurityMockMvcRequestPostProcessors.jwt()),
          # wiec pod ten adres nikt nie zaglada, ale JwtDecoder musi sie zbudowac.
          issuer-uri: http://localhost:8180/realms/truckmanager
          jwk-set-uri: http://localhost:8180/realms/truckmanager/protocol/openid-connect/certs
